# .env.example
# The root URL of the deployed backend API (e.g., https://glamsphere-api.onrender.com/api)
# If left blank (e.g. for GitHub Pages demo), the frontend will run in "demo mode" using local sample data.
VITE_API_URL=http://localhost:5000/api
